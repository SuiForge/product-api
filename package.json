{
  "name": "product-api-wallet-auth",
  "private": true,
  "type": "module",
  "scripts": {
    "build:wallet-auth": "node scripts/build-wallet-auth.mjs",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed"
  },
  "dependencies": {
    "@mysten/dapp-kit-core": "^1.1.4",
    "@mysten/sui": "^2.6.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "esbuild": "^0.25.10"
  }
}
